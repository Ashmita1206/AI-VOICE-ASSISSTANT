[pytest]
markers =
    notepad: marks tests that control the Notepad application (require Windows + display)
    slow: marks tests with long runtimes (multi-step sequences)
    integration: marks integration tests that read live Notepad content
